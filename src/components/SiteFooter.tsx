export function SiteFooter() {
  return (
    <footer className="border-t border-border bg-secondary/40">
      <div className="mx-auto flex max-w-7xl flex-col gap-2 px-4 py-8 text-sm text-muted-foreground sm:flex-row sm:items-center sm:justify-between">
        <p>AgroTicker — near real-time commodity tracking for Brazilian agriculture.</p>
        <p>Reference prices are indicative and for informational use only.</p>
      </div>
    </footer>
  );
}
